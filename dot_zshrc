#zmodload zsh/zprof

[[ -f /etc/profile ]] && source /etc/profile
source ~/.vendor/antidote/antidote.zsh

source ~/.zsh/plugins.zsh

source ~/.zsh/env.zsh
source ~/.zsh/options.zsh
source ~/.zsh/functions.zsh
source ~/.zsh/alias.zsh

[[ -x "$(command -v neofetch)" ]] && neofetch -s

#zprof
